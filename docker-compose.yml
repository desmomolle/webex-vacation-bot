services:
  webex-vacation-bot:
    build: .
    container_name: webex-vacation-bot
    restart: unless-stopped
    env_file: .env
    ports:
      - "${WEB_PORT:-8080}:8080"
    volumes:
      - ./data:/data
    networks:
      - shared-services

networks:
  shared-services:
    external: true
